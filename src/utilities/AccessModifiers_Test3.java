package utilities;

import day40_FinalKeyword.ProtectedAccessModifier;

public class AccessModifiers_Test3 {

    public static void main(String[] args) {

     //   System.out.println(ProtectedAccessModifier.name1); default is not visible outside the package

    //    System.out.println(ProtectedAccessModifier.name2); protected access modifier is not always visible outside the package.only time protected is visible outside the package is the class should be the subclass

    }
}
